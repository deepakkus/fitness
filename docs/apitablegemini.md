| No | API Name | API Route | Tables/Views Name | API Type | Param Type | Required Parameter | Optional Parameter | Response - 200 | Response 404 | Response 500 | Notes/ Comments | Questions to Ask to Client |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Get All Activities | `/api/activities` | activities, users, ref_activity_types, activity_media, activity_members | GET | None | None | None | Array of Activity objects (including organizer details, activity type, media, and member count) |  | Internal Server Error | Returns a list of all activities with associated data. Includes image URLs constructed using the base URL.  | How should "available_spots" be calculated when `max_participants` is null?  |
| 2 | Get Activities by Type | `/api/activities/[activity_type_id]` | activities, users, ref_activity_types, activity_media, activity_members | GET | `activity_type_id`: string | `activity_type_id` | None | Object containing `items` (count), `type` ("activity"), and `data` (array of Activity objects) |  | Internal Server Error | Returns activities filtered by `activity_type_id`. Currently unprotected, but can be easily protected by uncommenting the token check. |  |
| 3 | Get Activity Types | `/api/activity-type` | ref_activity_types | GET | None | None | None | Array of Activity Type objects |  | Internal Server Error | Returns all activity types. Currently unprotected, but can be protected. |  |
| 4 | Accept/Reject Activity Join Request | `/api/activity_accept/[requestId]` | activity_join_requests, activities, activity_members | POST | `requestId`: string | `requestId`, `action` (query param: "accept" or "reject") | None | Success message with updated request data or error message if activity has started, is full, or request not found | Invite request not found or you are not authorized | Internal Server Error | Protected route. Processes user's accept/reject action on an activity join request. Handles checks for past events, full activities, and user authorization. Updates join request status and returns a success or error message. |  |
| 5 | Invite User to Activity | `/api/activity_invite/[activityId]` | activity_join_requests, activities, activity_members | POST | `activityId`: string | `activityId`, `invitee_id` (in body) | None | Success message with invite request data or error message if activity has started, is full, user is not a member, or invitee is already a member/invited | Activity not found | Internal Server Error | Protected route. Sends an invitation to a user to join an activity. Performs various checks before creating the invite in the `activity_join_requests` table. Returns a success message.  |  |
| 6 | Request to Join Activity | `/api/activity_join/[activityId]` | activity_join_requests, activities, activity_members | POST | `activityId`: string | `activityId` | None | Success message with join request data or error message if activity has started, is full, or user is already a member/requested | Activity not found | Internal Server Error | Protected route. Allows a user to request joining an activity. Handles checks for past events, full activities, and existing membership/requests.  |  |
| 7 | Get Activity Notifications | `/api/activity_notifications` | activity_notifications, users, activities | GET | None | None | None | Grouped notifications object (message, comment, like, activity_join) | Unauthorized access | Internal Server Error | Protected route. Retrieves notifications for the authenticated user, groups them by type, and processes them to include user names and activity titles. | Confirm the logic for filtering notifications (especially the OR condition and exclusion of self-trigered notifications). |
| 8 | Get Activity Join Requests | `/api/activity_requests` | activity_join_requests, users, activities, votes, activity_media | GET | None | None | None | Object containing `voting`, `pending`, `accepted`, and `you_sent` arrays of requests | No activities found for the user | Internal Server Error | Protected route. Retrieves various types of join requests (pending, accepted, sent) related to the user and the activities they are members of. Includes vote data and user details.  | Clarify the difference between the `accepted` and `you_sent` arrays (which requests do they represent?). |
| 9 | NextAuth Authentication | `/api/auth/[...nextauth]` |  | GET, POST | Depends on provider | Depends on provider | Depends on provider | Depends on provider | Depends on provider |  | Handles authentication using NextAuth.js. | What authentication providers are used? |
| 10 | Get Events by User | `/api/event/[userId]` | activities, activity_join_requests, activity_media | GET | `userId`: string | `userId` | None | Object containing `items` (count), `type` ("event"), and `data` (array of Event objects)  |  | Failed to fetch events | Retrieves events created by a specific user. |  |
| 11 | Create Event | `/api/event/create` | activities | POST | None | `title`, `activity_type_id`, `location`, `start_time` (in body) | `sub_title`, `description`, `end_time`, `rules`, `contact_info`, `url` | Success message with the created event object | Missing required fields | Internal Server Error | Protected route. Creates a new event. Basic input validation included. | What fields should be required? What should the maximum length of text fields be? Should the API endpoint return the full event data or just a success/failure response?  |
| 12 | Upload Image | `/api/images` | users, users_blobs, achievement_media, achievement_media_blobs, activity_media, activity_media_blobs, message_media, message_media_blobs | POST | FormData with `image` or `imageList`, `bucket_name` | `image` or `imageList`, `bucket_name`  | `message_id`, `activity_id`, `achievement_id` (depending on `bucket_name`) | Success message with filePath, or error if image processing fails or image is too large | Image Route- Missing Image, Image Route- Missing bucket_name, Image Route - Missing message_id, activity_id, or achievement_id, Invalid bucket_name  | Failed to process image. Missing Blob, Failed to process image. image exceeded Size despite compression efforts, Error uploading image | Protected route. Uploads and processes images (resizing, compression). Stores images in different "buckets" (profiles, activities, messages). Image data is stored in blob tables. |  |
| 13 | Get Image | `/api/images/[...path]` | users, users_blobs, activity_media, activity_media_blobs, achievement_media, achievement_media_blobs, message_media, message_media_blobs | GET | `path`: string[] | `path` | None | Image blob data | Invalid path, Image not found |  | Retrieves image data from blob storage. Uses a `TABLE_MAP` to determine the correct Prisma model based on the path. Caches images aggressively. |  |
| 14 | Get Message by ID | `/api/messages/[messageId]` | activity_messages_view | GET | `messageId`: string | `messageId` | None | Object containing an array of formatted messages | Params Missing messageId | Failed to fetch messages | Protected route. Retrieves message details by message ID from the `activity_messages_view` view.  |  |
| 15 | Get Message Groups | `/api/messages/groups` | activity_members_messages_view | GET | None | None | None |  Object containing an array of Group objects. Each group includes details about the activity, admin, and last message. | Unauthorized access | Failed to fetch groups | Protected route. Retrieves a list of message groups for the current user based on their activity memberships. Uses a view for efficient querying. | What should the format for last message be? Should the user name or ID be included? What if there are no messages in a group? |
| 16 | Get Messages by Activity | `/api/messages/groups/[activityId]` | activity_messages_view | GET | `activityId`: string | `activityId` | None | Object containing an array of messages for the specified activity | Params Missing activityId | Failed to fetch messages | Protected route. Retrieves messages for a specific activity using the `activity_messages_view` view. Formats messages to include sender information and media URLs. |  |
| 17 | Send Message | `/api/messages/send` | messages | POST | None | `activity_id`, `message` (in body) | None | Success message and the newly created message object | Missing required fields activity_id | Internal Server Error | Protected route. Sends a message to an activity. Returns a success message and new message details. |  |
| 18 | Get Posts by User | `/api/post/[userId]` | activities, activity_join_requests, activity_media | GET | `userId`: string | `userId` | None |  Object containing items count, type "post", and an array of posts. | | Failed to fetch posts | Retrieves posts created by a specific user. | |
| 19 | Update Profile | `/api/profile` | users | POST | FormData with `username`, `name` | `username`, `name` | `location`, `age_group`, `about_me` | Success message with updated user data | Missing parameters | Error uploading Profile Information | Protected route. Updates user profile information. | What data validation should be performed on the input fields? |
| 20 | Get Public Activity Details | `/api/public/activities/[activityId]` | activities, users, activity_media, activity_comments, activity_join_requests | GET | `activityId`: string, query params `event` or `post` | `activityId` | `event`, `post` (query params: 1 or null) | Object with activity details (including organizer, media, comments, interested count) |  | Failed to fetch activities | Public route. Retrieves details of a specific activity, either an event or a post, based on the `activityId` and query parameters. |  |
| 21 | Add/Get Comments on Activity | `/api/public/activities/[activityId]/comments` | activity_comments, users | POST, GET | `activityId`: string (for both), `comment`, `rating` (in body for POST) | `activityId` (for both), `comment`, `rating` (for POST) | None | For POST: new comment data. For GET: object with comments count, type, and comment data array. | Params Missing activityId | Failed to add comment/Internal Server Error | Protected route for POST (adding comments). Public route for GET (retrieving comments). Adds a new comment to an activity or retrieves all comments for an activity. |  |
| 22 | Dislike Activity | `/api/public/activities/[activityId]/dislike` | activity_likes | POST, GET | `activityId`: string | `activityId` | None | Message indicating dislike status (added/removed) (POST), count of dislikes (GET) | Params Missing activityId | Internal Server Error | Protected route for POST, public route for GET. Adds or removes a dislike from an activity (POST), retrieves the dislike count for an activity (GET). |  |
| 23 | Like Activity | `/api/public/activities/[activityId]/like` | activity_likes | POST, GET | `activityId`: string | `activityId` | None | Message indicating like status (added/removed) (POST), count of likes (GET) | Params Missing activityId | Internal Server Error | Protected route for POST, public route for GET. Adds or removes a like from an activity (POST), retrieves the like count for an activity (GET). |  |
| 24 | Get Current User Profile | `/api/user/me` | users, activities, user_followers | GET | None | None | None | User data object with counts for followers, following, posts, and events | Unauthorized access, User not found | Failed to fetch user profile | Protected route. Retrieves the current user's profile information, including follower/following counts and post/event counts. |  |
| 25 | Follow User | `/api/user_follow/[followeeId]` | user_followers | POST | `followeeId`: string | `followeeId` | None | Success message and follow data | Params Missing followee_id, You cannot follow yourself, You are already following this user | Internal Server Error | Protected route. Follows a user. Prevents a user from following themselves and checks for existing follow relationships. |  |
| 26 | Vote on Join Request | `/api/vote/[requestId]` | votes | POST | `requestId`: string, `action` (in body: "approve" or "ignore") | `requestId`, `action` | None | Success message indicating vote status (cast/updated) | Unauthorized access, Invalid action | Internal Server Error | Protected route. Allows a user to vote on a join request. Handles updating existing votes and creating new votes. | What is the expected behavior when a vote changes the outcome of a join request (e.g., enough approvals to accept)?  |